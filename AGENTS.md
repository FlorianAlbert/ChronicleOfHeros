## Agent skills

### Issue tracker

Issues and PRDs are tracked as GitHub issues. See `docs/agents/issue-tracker.md`.

### Domain docs

The repository uses a single-context domain-documentation layout. See `docs/agents/domain.md`.

### Architecture

Architecture work -- capability boundaries, dependency injection, persistence, migrations, database tests, or C# extensions -- follows `docs/agents/architecture.md`.
